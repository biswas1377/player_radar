# Environment Variables Template for PlayerRadar Server
# Copy this file to .env and fill in your actual values
# DO NOT commit the actual .env file to version control

# JWT Secret for token signing (generate using: node generate-jwt-secret.js)
JWT_SECRET=your_jwt_secret_here

# MongoDB Connection String
MONGODB_URI=mongodb://localhost:27017/playerradar

# Server Port
PORT=3001

# Node Environment (development, production, test)
NODE_ENV=development

# Email Configuration (required for player notifications)
# For Gmail, you'll need to:
# 1. Enable 2-factor authentication
# 2. Generate an "App Password" 
# 3. Use your Gmail address and the app password here
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password-here

# For other email providers, update the email service configuration
# in server/services/emailService.js
